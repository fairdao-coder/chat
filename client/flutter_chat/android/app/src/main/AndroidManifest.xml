<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 基礎網絡權限：HTTP 請求與 SignalR 信令全部依賴它。
         注意：debug / profile 變體自帶此權限，但 release 只繼承 main/，
         缺少它的話 release APK 安裝後完全無法聯網，登錄與收發消息會全部失敗。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 录音发送：麦克风权限（record 包必需）。
         neverForLocation 表示不用于定位，符合 Android 12+ 最佳实践。 -->
    <uses-permission
        android:name="android.permission.RECORD_AUDIO"
        android:usesPermissionFlags="neverForLocation"
        android:label="@string/microphone_usage" />

    <!-- 视频通话：摄像头权限（flutter_webrtc 必需）。 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 开发期允许访问 http:// 后端（如 localhost / 内网 IP）。
         生产环境应改为 https 并移除该属性。 -->
    <application
        android:label="My Chat"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 配置深链：fairchat://config?name=品牌名&api=服务器地址
                 分享该链接给用户，点开后登录页弹确认框一键完成配置。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="fairchat" android:host="config" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
